{
  "name": "tidal-trading-agent",
  "main": "src/cli.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env src/cli.ts",
    "dash": "node --env-file-if-exists=.env src/index.ts",
    "test": "node --env-file-if-exists=.env --test"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
