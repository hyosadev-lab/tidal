{
  "name": "tidal-trading-agent",
  "main": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env index.ts",
    "test": "node --test"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
