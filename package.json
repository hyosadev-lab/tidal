{
  "name": "tidal-trading-agent",
  "main": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env index.ts",
    "dash": "node --env-file-if-exists=.env server.ts",
    "test": "node --env-file-if-exists=.env --test"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
